services:
    redis-stack:
        image: redis/redis-stack:latest
        container_name: redis-stack
        ports:
            - "6379:6379"
            - "8001:8001"
        volumes:
            - redis_cont:/var/lib/redis/

    # ollama:
    #     image: ollama/ollama:latest
    #     container_name: ollama
    #     ports:
    #         - "11434:11434"
    #     volumes:
    #         - ollama:/root/.ollama
    #     networks:
    #         - docs-support-agent-network

    # qdrant:
    #     image: qdrant/qdrant:latest
    #     container_name: qdrant
    #     ports:
    #         - "6333:6333"
    #     volumes:
    #         - qdrant-vlm:/qdrant/storage

volumes:
    redis_cont:
        external: true
    # ollama:
        # external: true
    # open-webui:
        # external: true
    # qdrant-vlm:
        # external: true
# networks:
    # docs-support-agent-network:
        # external: true
